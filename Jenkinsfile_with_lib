@Library('my_lib@main') _

pipeline {
    agent none
    stages {
        stage('run docker') {
            agent any
            steps {
                dockerRunCmd('ps')
            }
        }
    }
}
